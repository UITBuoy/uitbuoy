query CourseMakeUpClass($courseCode: String) {
    makeUpClass(courseCode: $courseCode) {
        classId
        classroom
        courseCode
        createdDate
        end
        start
        time
        title
    }
}
